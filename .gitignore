node_modules/
dist/
coverage/
.env
.env.local
*.log
.DS_Store
.cache/

# transient SQLite sidecars only - data/press.sqlite IS a deliverable (R24)
*.sqlite-journal
*.sqlite-wal
*.sqlite-shm
data/dev.sqlite

# Ollama response cache (content-addressed, regenerable)
.cache/

# Partial-run registry samples (regenerable dev artifacts, not deliverables)
data/companies.sample-*.json

# Production run log (regenerated; the artifacts it produces ARE committed)
data/backfill.log
apps/web/dist
